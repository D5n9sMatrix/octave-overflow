#!/usr/bin/ocatve

# The measurements of the earth's soils are on the edge of environmental
# rescues of the gravity of the pages selected for the impressions of your
# earth organism on formations of its environment reports printed from its
# earth pages for the organism of its center gravity.

function gravity(p2, e2, tf, x)

  p2 = 0
  e2 = 4
  x = [p2, e2, p2, e2, p2, e2]

  if p2 != e2
     disp("gravity earth about organism on formations")
     disp("gravity" + isvector(x))
  else
    tf = isvector(x)
  endif

end
